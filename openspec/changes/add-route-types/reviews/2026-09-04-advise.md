> **ADVISE:** send-back
> **READER:** sol-arch-review
> **SPAWN:** /home/dorje/work/WorldTree/Dreamball/.spawns/add-route-types-1788554876-564698-c607dfbc

# Architecture review: add-route-types

## Independent take — blind pass

1. Pin `fixtures/goldens/manifest.json` as the sole gate for identikey-protocol encode/decode coverage.
2. Pin the core partition to types owned by identikey-protocol, not every route destination.
3. Refuse `Memory` in the core golden set because Memory is an archiform destination.
4. Refuse `Layout` in the core golden set for the same ownership reason.
5. Refuse `TrustObservation` unless a current decision explicitly reverses the steer override.
6. Do not infer protocol ownership from fixtures merely residing in this repository.
7. Keep excluded destinations visible in a non-core partition where cross-boundary examples remain useful.
8. Require the manifest and README partition descriptions to agree unambiguously.
9. Require golden movement to preserve the distinction between protocol validation and contested examples.
10. Tradeoff: a smaller core weakens apparent route breadth but prevents identikey-protocol from acquiring foreign canonical types.

## Gate finding

Architecturally, the core golden set **may exclude Memory, Layout, and TrustObservation**. The packet establishes that Memory and Layout belong to archiform and that TrustObservation was displaced by a steer override. Since only `fixtures/goldens/manifest.json` gates identikey-protocol encode/decode, none of those destinations should remain core merely to demonstrate broader route coverage.

## Reason for send-back

I cannot complete the required evidence comparison or persist the review under the available read-only interface. In particular, I could not inspect the living spec, cited files, `design.md`, or `tasks.md`, nor verify that the authored change answers the concerns above. This verdict is therefore procedural rather than an objection to excluding the three types. No product code, banner, task state, or fold was changed.
