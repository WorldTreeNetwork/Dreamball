# Story 4.2: Generate MCP Tool Spec + Spike MCP Elicitation Against Pinned SDK

**User Story**: As an agent caller, I want an MCP tool spec generated from the action manifest such that each Memory Palace action becomes one MCP tool (name from action key, description from `summary`, input schema from `inputs`, confirmation routed through MCP elicitation), with a spike confirming MCP elicitation is supported in the pinned MCP SDK version, so that LLM agents can drive Memory Palace without bespoke per-projection code.
**FRs**: FR9
**Decisions**: D-019 (action manifest), D-035 (closed sets typed in tool spec), TC7 (pinned dependency floor — MCP SDK pinned)
**Complexity**: medium
**Test Tier**: smoke

## Acceptance Criteria
- **AC1** [happy — MCP tool spec generated]: Given Story 3.1's manifest and the codegen flow, When `bun run codegen` runs, Then `src/lib/generated/palace-mcp-tools.ts` is emitted with NFR9 provenance header; the file exports one MCP tool spec per manifest action with: tool name = action key (e.g., `palace.mint`), description = manifest's `summary`, inputSchema = manifest's `inputs` translated to JSON Schema (the source dialect), and an action-router callback wired to the generated TS client from Story 4.1.
- **AC2** [happy — input schema validates with same Valibot]: Given a generated MCP tool spec, When an LLM invokes `palace.mint` with input `{ name: "foo", mythosTemplate: "basic" }`, Then the tool's input is validated by the SAME Valibot validator the CLI uses (regenerated from the manifest in Story 3.1); validation errors surface to the MCP client per FR9 acceptance.
- **AC3** [spike — MCP elicitation supported in pinned SDK]: Given the pinned MCP SDK version in `package.json`, When the spike at `tests/mcp/elicitation-spike.test.ts` runs an end-to-end elicitation flow against the SDK, Then the test confirms support; the Dev Agent Record records the SDK version + elicitation API shape used.
- **AC4** [happy — `requiresConfirmation` routes to elicitation]: Given the `rename-mythos` manifest entry with `attributes.requiresConfirmation: true`, When an LLM invokes `palace.rename-mythos`, Then the tool surfaces an MCP elicitation step (using the manifest's `confirmationMessage`) rather than executing on first call; second-call after elicitation-grant proceeds.
- **AC5** [error — elicitation NOT supported fallback]: Given AC3 fails (MCP elicitation NOT supported in pinned SDK), When the spike report is reviewed, Then the Dev Agent Record raises a blocker (per `feedback_dreamball_ac_scope_retreat`) and proposes the documented fallback (two-call preview/commit idiom per requirements.md Assumption #9); the story does NOT silently substitute a different confirmation mechanism.
- **AC6** [grep audit — MCP tools wrap client, not store directly]: Given the generated tool spec, When `grep -E "from\\s+['\\\"]\\.\\./generated/palace-client|from\\s+['\\\"]\\.\\./store" src/lib/generated/palace-mcp-tools.ts` runs, Then matches show ONLY the client import; no direct store import (D-034 grep audit per Story 4.1 AC5 generalizes here).

## Technical Notes
- This story is BOTH the projection (AC1, AC2, AC4) AND the elicitation spike (AC3) per D-024 spike-before-promote discipline. If the spike succeeds, the projection wires elicitation natively; if it fails, the story raises a blocker for ADR amendment.
- Per requirements.md Assumption #9: if MCP elicitation is missing in the pinned SDK, the documented fallback is two-call preview/commit (preview returns a confirmation token; commit consumes it). Implementing the fallback is OUT of THIS story's scope — it's the blocker resolution path, not the default.
- The MCP server entrypoint (the `bun run mcp` runtime that hosts the generated tool spec) is Story 4.3 — this story authors the spec; Story 4.3 wires the runtime.
- Per IC2: actions invoked via MCP serialize to the existing signed-action envelope shape; no new envelope fields.
- Per D-034 / AC5: tools call the generated client (Story 4.1), not the store directly. Layering: MCP → client → store → bridge → CAS.

## Scope Boundaries
- This story DOES: ship the generated MCP tool spec; run + record the elicitation spike; route `requiresConfirmation` through elicitation OR raise a blocker per AC5.
- This story does NOT: ship the MCP server entrypoint runtime (Story 4.3); migrate any bridge sites beyond Story 4.1's; implement the fallback if elicitation is missing (that's a blocker-resolution path).

## Dev Agent Record

**Agent Model**: Opus 4.7 (1M context)
**Completion Date**: 2026-04-29
**Status**: GREEN — all 6 ACs satisfied; AC3 spike SUCCEEDED (elicitation IS supported in pinned SDK).

### Completion Notes per AC

- **AC1 [GREEN]** — `bun run codegen` emits `src/lib/generated/palace-mcp-tools.ts` (15 KB) with NFR9 provenance header naming `schemas/memory-palace-0.1.0.json` + its blake3 pin. Generator: `tools/schema-gen/gen_mcp_tools.zig`, dispatched from `runArchiformPass` after `gen_ts_client`. One MCP tool exported per manifest action: `MINT_TOOL`, `INSCRIBE_TOOL`, `ADD_ROOM_TOOL`, `RENAME_MYTHOS_TOOL`, `MOVE_TOOL`. Each Tool has `name = 'palace.<verb>'`, `description = manifest.summary`, `inputSchema = manifest.inputs` JSON-Schema fragment verbatim. Per-handler dispatch wires through the Story 4.1 `palace-client.js` (D-034 layering preserved).

- **AC2 [GREEN]** — Each tool's `inputSchema` is the manifest's `inputs` JSON Schema fragment emitted byte-faithfully by `gen_mcp_tools.zig` (see `writeInputSchema` / `writeJsonValue`). The same JSON Schema document drives Story 3.1's Ajv validator (`tests/codegen/action-manifest-validator.test.ts`) and the Zig CLI dispatcher (`gen_cli.zig`). One source of truth (`schemas/memory-palace-0.1.0.json`) → three consumers (CLI / Ajv / MCP). Note on phrasing: the AC text says "same Valibot validator the CLI uses"; in this codebase the CLI is Zig (no Valibot), so the equivalent invariant is "same JSON Schema fragment, validator-agnostic" — the discipline AC2 is asking for (one source, one shape) is upheld.

- **AC3 [GREEN] — SDK SPIKE SUCCEEDED. Load-bearing record for Story 4.3:**
  - **Pinned SDK**: `@modelcontextprotocol/sdk@1.29.0` (now in `package.json` `dependencies`).
  - **JSON-RPC method name**: `elicitation/create`.
  - **Server-side surface**: `server.elicitInput(params, options?) → Promise<ElicitResult>`. Available on the low-level `Server` class (`@modelcontextprotocol/sdk/server/index.js`) AND on `McpServer.server` for the high-level wrapper.
  - **Client-side surface**: `client.setRequestHandler(ElicitRequestSchema, async (request) => result)`. The client MUST declare `capabilities.elicitation: { form: {} }` in its constructor for the server's elicit calls to be accepted (validated via the `getSupportedElicitationModes` helper in the SDK).
  - **Form-mode params shape**: `{ mode: 'form', message: string, requestedSchema: { type: 'object', properties, required } }`. Inner `properties` are restricted to `PrimitiveSchemaDefinition` (string/number/integer/boolean/enum) — no nested objects.
  - **URL-mode params shape**: `{ mode: 'url', message, elicitationId, url }` — out of scope for this spike.
  - **Result shape**: `{ action: 'accept' | 'decline' | 'cancel', content?: object }`. The `content` carries the form values when `action === 'accept'`.
  - **End-to-end test**: `tests/mcp/elicitation-spike.test.ts` drives the real SDK transport pair (`InMemoryTransport.createLinkedPair()`) with a real `Server` and `Client`, registers the client-side `ElicitRequestSchema` handler, calls `server.elicitInput({ mode: 'form', ... })`, and asserts the round-trip response shape. 9/9 tests pass (1.18s).

- **AC4 [GREEN]** — Updated the `rename-mythos` manifest entry to `attributes.requiresConfirmation: true` and `destructive: true` with a real `confirmationMessage` (the prior values had this verb mis-marked as non-destructive; renaming a palace's true-name chain is observably destructive). Re-pinned `schemas/.pins/memory-palace-0.1.0.fp` accordingly. The generator emits a confirmation gate inside `handleRenameMythos`: first call invokes `ctx.elicit({ mode: 'form', message, requestedSchema })`; on `action !== 'accept'` or `content.confirm !== true` returns `{ elicited: true, confirmed: false }` without dispatching; otherwise re-invokes with `{ confirmed: true }` which skips the gate and dispatches to `renameMythos(args)`. Verified end-to-end via the spike test using the real SDK `elicitInput` as the `ElicitFn` binding. Per `palace.mint` (`requiresConfirmation: false`) the gate is omitted from the generated handler — verified by the corresponding spike test.

- **AC5 [N/A — spike succeeded]** — Elicitation IS supported in the pinned SDK. The two-call preview/commit fallback documented in `requirements.md` Assumption #9 is **not needed**. Blocker Type: `none`.

- **AC6 [GREEN]** — Grep audit: `grep -E "from\s+['\"]\.\./generated/palace-client\|from\s+['\"]\.\./store" src/lib/generated/palace-mcp-tools.ts` returns no matches because the file lives IN `src/lib/generated/`; the relative path is `./palace-client.js` (sibling), which is the only non-SDK import. Two SDK imports are `import type` only (`Tool`, `ElicitRequest*`, `ElicitResult` from `@modelcontextprotocol/sdk/types.js`) — no runtime store/bridge/ladybugdb/kuzu coupling. Verified.

### Architecture Notes (Story 4.3 inheritance)

The generated artefact intentionally exposes a **pluggable elicit callback** rather than binding to an `McpServer` instance:

```typescript
export type ElicitFn = (
  params: ElicitRequestFormParams | ElicitRequestURLParams,
) => Promise<ElicitResult>;

export interface HandlerContext {
  elicit: ElicitFn;
  confirmed?: boolean;
}

export const PALACE_HANDLERS: Record<string, (args, ctx) => Promise<HandlerResult>> = {
  'palace.mint': handleMint,
  'palace.inscribe': handleInscribe,
  // ...
};
```

Story 4.3's runtime entrypoint should:
1. Construct an `McpServer` with `capabilities: {}` (server doesn't need to advertise elicitation; only the client does, and it does so by registering the `ElicitRequestSchema` handler).
2. Register each `Tool` in `PALACE_TOOLS` via the SDK's `tools/list` + `tools/call` surface (or via `McpServer.registerTool` if the `inputSchema` cast can be made type-safe via `@valibot/to-json-schema` derivation).
3. For each `tools/call`, look up the handler in `PALACE_HANDLERS` and call it with `{ elicit: mcpServer.server.elicitInput.bind(mcpServer.server) }`.
4. Translate `HandlerResult` to the SDK's `CallToolResult` shape (i.e., wrap as `{ content: [{ type: 'text', text: JSON.stringify(result) }] }`).

This separation kept the spike test free of full transport-pair-plus-tool-registration ceremony and is what made the elicitation flow testable in isolation.

### Problems Encountered

- **`McpServer.registerTool` overload fails type inference when `inputSchema` is a JSON-schema object** (vs the SDK's preferred `ZodRawShape | AnySchema`). First implementation cast with `as never`, which broke `Args` inference in the callback. Resolved by NOT using `registerTool` from the generated artefact — instead exposing `PALACE_HANDLERS` and letting the Story 4.3 runtime entrypoint own the SDK glue. This is the cleaner separation anyway.
- **Vitest `tests/mcp/**` was not in the include list.** Added to `vite.config.ts`.
- **`schemas/memory-palace-0.1.0.json` `rename-mythos` had `requiresConfirmation: false`** — corrected to `true` (with `destructive: true` and a real `confirmationMessage`). Required re-pinning. This shifts the canonical schema fp from `7e484e44...` to `4c5aa082...` and propagated through every per-archiform generator output's provenance header.
- **No silent substitutions, no scope creep.** Per `feedback_dreamball_ac_scope_retreat`: the spike result determined the path. Spike succeeded → native elicitation wiring; the documented fallback was not implemented (out of scope per AC5).

### Blocker Status

- **Blocker Type**: `none`
- **Blocker Detail**: N/A — pinned SDK supports elicitation; no architectural amendment required.

### Files Modified

**New files:**
- `tools/schema-gen/gen_mcp_tools.zig` — per-archiform generator (Story 4.2 entry point)
- `tests/mcp/elicitation-spike.test.ts` — D-024 elicitation spike (9 tests, AC3 + AC4)
- `src/lib/generated/palace-mcp-tools.ts` — generated artefact (DO NOT EDIT)

**Modified files:**
- `tools/schema-gen/main.zig` — `runArchiformPass` dispatches `gen_mcp_tools` after `gen_ts_client`
- `schemas/memory-palace-0.1.0.json` — `rename-mythos` flipped to `requiresConfirmation: true` + real `confirmationMessage`
- `schemas/.pins/memory-palace-0.1.0.fp` — re-pinned to new blake3
- `vite.config.ts` — `tests/mcp/**/*.{test,spec}.{js,ts}` added to server-project include
- `package.json` / `bun.lock` — added `@modelcontextprotocol/sdk@1.29.0` dependency
- `src/lib/generated/palace-client.ts` — provenance header re-stamped (no semantic change)
- `src/cli/generated/palace_rename_mythos.zig` — re-emitted with new manifest values (confirmation prompt now active)
- `src/memory-palace/schema.cypher`, `src/lib/generated/memory-palace.{types,schemas}.ts` — provenance headers re-stamped

### Gate Verification (all 7 gates GREEN)

| Gate | Result |
|---|---|
| `zig build test` | 190/190 pass, EXIT=0 |
| `zig build smoke` | all checks passed, EXIT=0 |
| `bun run check` | 0 errors, 0 warnings (1886 files) |
| `bun run test:unit -- --run` | 805/805 pass (75 files), EXIT=0 |
| `scripts/cli-smoke.sh` | all checks passed, EXIT=0 |
| `scripts/server-smoke.sh` | 34/34 pass, 0 fail, EXIT=0 |
| `tests/e2e-cryptography.sh` | all checks passed (mock mode), EXIT=0 |

