# Story 7.2: Update `docs/ARCHITECTURE.md` Cross-Runtime Invariant + Wasm Action Host + Codegen Flow Diagram + Cross-Doc Consistency Pass

**User Story**: As a future agent onboarding to Dreamball post-sprint-002, I want `docs/ARCHITECTURE.md` to reflect the refined two-part cross-runtime invariant ("encoding algorithm in Zig + golden vectors; field shapes in JSON Schema") with new sections / ADR entries for the wasm action host architecture, the `dreamball.*` import seam, and the codegen flow, plus a cross-doc consistency pass ensuring CLAUDE.md, PROTOCOL.md, ARCHITECTURE.md, and dreamball-imports.md are mutually consistent, so that the architectural pivot is fully landed in the docs tree without drift.
**FRs**: FR15
**Decisions**: D-018 (cross-runtime invariant refinement), D-020 (wasm runtime), D-025 (forward-declare contracts), D-029 (pin format), D-031 (trust model), D-032 (single shared host), D-033 (5-import surface), TC9 (recrypt naming review)
**Complexity**: medium
**Test Tier**: smoke

## Acceptance Criteria
- **AC1** [happy — cross-runtime invariant refined]: Given `docs/ARCHITECTURE.md`, When the cross-runtime invariant section is read, Then it documents the two-part invariant: (i) "encoding algorithm in Zig + golden vectors" (CBOR algorithm canonical in Zig per D-018), (ii) "field shapes in JSON Schema" (vendored + fp-pinned per D-018, D-029); the prose explicitly cites D-018, D-029, D-032.
- **AC2** [happy — wasm action host ADR / section per D-020 + D-032]: Given the doc, When the wasm action host section is read, Then it documents: single shared Zig host code targeting CLI (Zig+WASI) and (sprint-003+) browser per D-032; verify-before-instantiate via blake3 per SEC4; memory budget per NFR7; structured invocation events per NFR11; cross-references to `docs/dreamball-imports.md` (Story 5.5).
- **AC3** [happy — codegen flow diagram]: Given the doc, When the codegen flow section is read, Then it includes a diagram (ASCII / mermaid) showing: `schemas/<archiform>-<version>.json` → `schemas/.pins/<archiform>-<version>.fp` → `tools/schema-gen/main.zig` → 5 per-target generators (`gen_zig`, `gen_ts`, `gen_valibot`, `gen_cbor`, `gen_cypher`) → `src/lib/generated/*` + `src/memory-palace/schema.cypher`; pin verification step shown.
- **AC4** [happy — cross-doc consistency pass]: Given CLAUDE.md, PROTOCOL.md, ARCHITECTURE.md, dreamball-imports.md, When all four are read together, Then: (i) the cross-runtime invariant prose is consistent across CLAUDE.md and ARCHITECTURE.md (no contradiction), (ii) PROTOCOL.md's pointer to JSON Schema files matches ARCHITECTURE.md's codegen flow, (iii) ARCHITECTURE.md cites `docs/dreamball-imports.md` for the wasm-host-import contract, (iv) PROTOCOL.md's wasm trust-model subsection (Story 7.1) and ARCHITECTURE.md's wasm host section reference the same D-031 trust chain.
- **AC5** [happy — D-NEW promotions cited]: Given the doc, When the architectural decisions log is reviewed, Then it cites the promoted decisions D-021 (LadybugDB transactional model), D-022 (bridge pattern), D-023 (signActionEnvelope), D-024 (spike-before-promote), D-025 (forward-declare contracts), D-028 (Triple-native KG) — each with a one-paragraph "what changed" entry.
- **AC6** [happy — sentinel debt closure cited]: Given the doc, When the sentinel-substitution debt is referenced, Then it cites Stories 6.1 + 6.2's closure (per FR13 + SEC3) and forward-points to the security pass for PQ dual-sig (per SEC6).
- **AC7** [happy — markdown lint + link checker pass]: Given the updated doc + cross-doc consistency, When `bun run lint:md` and a link checker run across all four docs, Then all pass.
- **AC8** [happy — full gate set remains green]: Given the doc-only changes, When the full gate set runs (per TC2), Then all 7 gates remain green — doc changes should not regress any functional gate; if one breaks, investigate the undocumented assumption per Cluster H test-tier note.
- **AC9** [happy — recrypt naming review per TC9]: Given any new vocabulary, When cross-checked against `../recrypt/docs/wire-protocol.md`, Then no novel terminology where recrypt has an aligned term; commit message carries "recrypt naming reviewed: ✅".

## Technical Notes
- This is the CLOSING story of Cluster H and the FINAL story of the sprint (per Epic 7 dependency: depends on Epics 1–6). Sequence Phase 4 dispatch accordingly.
- The codegen flow diagram (AC3) MUST be a textual artifact (ASCII or mermaid) so it survives `git diff` review; do NOT use binary images.
- Cross-doc consistency (AC4) is a manual review pass — there is no automated tool. Reviewer reads all four docs in one sitting and confirms the four-way consistency invariants.
- Per Cluster H test tier: smoke = markdown lint + link checker + manual review. AC8's full-gate run guards against doc changes accidentally including code examples that break compilation.
- If any cross-doc inconsistency is discovered DURING the review pass, the inconsistency is fixed in THIS story's commit (small surface) — not punted to a follow-on.

## Scope Boundaries
- This story DOES: update ARCHITECTURE.md per AC1–AC3; cross-doc consistency pass per AC4; cite promoted decisions; close sentinel-debt reference; lint + link check + full-gate verify.
- This story does NOT: change PROTOCOL.md (Story 7.1); change `docs/dreamball-imports.md` (Story 5.5); change CLAUDE.md (already pre-sprint); ship any code.
