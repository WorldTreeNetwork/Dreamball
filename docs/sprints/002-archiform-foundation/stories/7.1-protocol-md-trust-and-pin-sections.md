# Story 7.1: Update `docs/PROTOCOL.md` — Schema Vendoring + Pin Format + Wasm Trust Model + JSON Schema Pointers

**User Story**: As a future agent reading the docs tree, I want `docs/PROTOCOL.md` to point at the JSON Schema files as the authoritative shape source, document the "Schema vendoring and pin format" subsection per D-029, document the "Wasm module signing and trust model" subsection per D-031, and reflect D-026 (surface registry) + D-027 (coord frames) ADRs in the relevant sections, so that the protocol document accurately describes the post-pivot architecture without requiring sprint-002 decision notes for context.
**FRs**: FR15
**Decisions**: D-018 (PROTOCOL.md points to JSON Schema), D-026 (§13.7 surface registry wording), D-027 (§13.2 coord frames citation), D-029 (pin format subsection), D-031 (wasm trust model subsection), D-035 (closed `attributes` / `effects.kind` enumeration), TC9 (recrypt naming review)
**Complexity**: medium
**Test Tier**: smoke

## Acceptance Criteria
- **AC1** [happy — JSON Schema pointer]: Given `docs/PROTOCOL.md`, When the field-shape section is read, Then it explicitly points at `schemas/root-2.0.0.json` and `schemas/memory-palace-0.1.0.json` as the authoritative shape sources; the prose preserves the "no hand-written schemas" wording (now phrased as "no hand-written schemas; JSON Schema vendored, generators consume it").
- **AC2** [happy — schema vendoring + pin format subsection per D-029]: Given the doc, When grepping for `## Schema vendoring and pin format` (or `### …`), Then the subsection is present and documents: the `schemas/<archiform>-<version>.json` location convention, the `schemas/.pins/<archiform>-<version>.fp` pin file format (single-line hex blake3), and the optional `bun run schemas:refresh` path.
- **AC3** [happy — wasm trust model subsection per D-031]: Given the doc, When grepping for `## Wasm module signing and trust model`, Then the subsection is present and documents: signed schema body + content-addressed wasm (transitive authentication), blake3 verify-before-instantiate (SEC4), the wasm-and-schema-lifecycle coupling (any wasm bug fix requires schema reissue), and the deferred per-module-signature option for sprint-003+.
- **AC4** [happy — §13.7 surface registry reflects D-026]: Given the §13.7 wording, When read, Then it documents: open-string surface; `scroll` baseline mandatory; optional `fallback` ordered-array attribute; per-lens registries; cycle behavior emits `surface-fallback-cycle` event and breaks to `scroll`.
- **AC5** [happy — §13.2 cites D-027]: Given the §13.2 coord-frames section, When read, Then it cites D-027 explicitly (or documents the polar-field + cartesian-placements + nested-reference-frames composition with reference to sprint-001's simplified path and Growth-tier `polarShellToCartesian` work).
- **AC6** [happy — closed sets enumerated per D-035]: Given the action-manifest section (added or extended in this story), When read, Then it enumerates the closed `attributes` set (`destructive`, `requiresConfirmation`, `confirmationMessage`, `agentVisible`) and the closed `effects.kind` enum (`ActionEnvelope`, `Read`, `Derived`); marks the `x-` prefix as reserved for sprint-003+.
- **AC7** [happy — markdown lint + link checker pass]: Given the updated doc, When `bun run lint:md` and a link checker run, Then both pass; broken citations or relative links fail.
- **AC8** [happy — recrypt naming review per TC9]: Given any new vocabulary introduced in the updates, When cross-checked against `../recrypt/docs/wire-protocol.md`, Then no novel terminology is introduced where a recrypt-aligned term exists; the diff carries a one-line "recrypt naming reviewed: ✅" note in the commit message.

## Technical Notes
- Surgical edits — this story EDITS specific sections; it does NOT rewrite PROTOCOL.md from scratch. Each AC names the section/subsection it touches.
- The wasm trust-model and pin-format subsections are NEW; the §13.2 / §13.7 / action-manifest updates are REVISIONS to existing sections.
- The full doc-tree consistency pass (cross-referencing ARCHITECTURE.md and `docs/dreamball-imports.md` from Story 5.5) lands in Story 7.2. THIS story focuses on PROTOCOL.md content.
- If a PROTOCOL.md change breaks a CI gate (e.g., a code example in the doc fails to compile), that is a signal of an undocumented assumption — investigate, do NOT bypass.
- Per Cluster H test tier: smoke = markdown lint + link checker + manual review.

## Scope Boundaries
- This story DOES: edit `docs/PROTOCOL.md` per AC1–AC6; pass markdown lint + link check; recrypt naming review.
- This story does NOT: rewrite ARCHITECTURE.md (Story 7.2); author `docs/dreamball-imports.md` (Story 5.5 ships it); change any code; change CLAUDE.md (already pre-sprint).
