# Story 1.1: Codegen Inversion Spike — Round-Trip Three Complex Zig Types

**User Story**: As a Dreamball maintainer, I want a spike that round-trips the three most expressively complex Zig types (envelope, sealed body, signature tier wrapper) through hand-authored JSON Schema and back to byte-identical Zig+TS+Valibot+CBOR outputs, so that Assumption #1 in `requirements.md` (JSON Schema draft 2020-12 can fully express the current Zig type system without expressivity loss) is validated before scaling to the full root.
**FRs**: FR1, FR2, FR3
**Decisions**: D-018, D-024 (spike-before-promote), D-030, D-035, NFR1
**Complexity**: large
**Test Tier**: thorough

## Acceptance Criteria
- **AC1** [happy — round-trip]: Given hand-authored JSON Schema fragments for `Envelope`, `SealedBody`, and `SignatureTierWrapper` (the three most expressively complex Zig types per Assumption #1), When the spike consumer in `tools/schema-gen/spike/main.zig` reads them and emits Zig + TS + Valibot + CBOR codecs to `tools/schema-gen/spike/out/`, Then the emitted Zig types compile and decode a sample envelope into the same in-memory representation as the legacy generator produces.
- **AC2** [byte-equivalence on the three types]: Given the spike outputs and the legacy generator outputs scoped to the same three types, When `diff -r tools/schema-gen/spike/out/ tools/schema-gen/legacy-spike-fixture/` runs, Then the diff is empty (byte-identical for the spike scope per NFR1, including provenance headers per NFR9).
- **AC3** [error — expressivity gap]: Given a Zig type construct that JSON Schema draft 2020-12 cannot natively express (e.g., a discriminated-union over CBOR tags), When the spike author encounters it, Then the spike report at `docs/decisions/2026-04-XX-codegen-spike-findings.md` records the construct, the chosen normalization (Schema dialect extension, naming convention, or `$comment` marker), and a Phase 3 follow-up note; the spike does NOT silently substitute (per `feedback_dreamball_ac_scope_retreat`).
- **AC4** [decision-promote]: Given the spike completes green, When the spike report is reviewed, Then a one-paragraph "spike outcome → architecture commitment" entry is appended to `docs/decisions/2026-04-XX-codegen-spike-findings.md` confirming D-018 + D-030 are sufficient (or proposing an amendment).

## Technical Notes
- Spike location is intentionally siloed (`tools/schema-gen/spike/`) so it does not perturb the legacy generator until Story 1.4's shadow phase wires both side-by-side.
- Use the existing `src/protocol_v2.zig` types as the legacy reference; copy the relevant struct decls into a `legacy-spike-fixture/` snapshot once at spike start so byte-equivalence diffing is reproducible.
- The spike's CBOR codec emission MUST defer to the canonical Zig CBOR encoding rules (D-018: "CBOR algorithm stays canonical in Zig"); the spike does NOT re-author CBOR encoding semantics.
- Include the `bun run codegen` runtime budget check (NFR2 ≤ 5 s) in the spike harness even at this scale — early signal on perf trajectory.

## Scope Boundaries
- This story DOES: validate Assumption #1 against the three named types; emit a spike report; commit the spike code under `tools/schema-gen/spike/` for follow-on stories to inherit.
- This story does NOT: author the full `schemas/root-2.0.0.json` (Story 1.2); build the production per-target generators outside `spike/` (Story 1.3); set up the shadow phase (Story 1.4); cut over (Story 1.5).

---

## Dev Agent Record

**Agent Model Used**: Opus 4.7 (1M context). This is the **restart** execution. A prior executor went off-spec and authored JSON Schemas for `aqueduct` / `mythos` / `trust-observation` (Memory Palace types belonging to Story 2.x). The restart deleted those off-spec schemas and re-authored against the three types named in the AC: `Envelope`, `SealedBody`, `SignatureTierWrapper`.

### Completion Notes per AC

- **AC1 (round-trip)** — green. The hand-authored Envelope / SealedBody / SignatureTierWrapper JSON Schemas declare `required` and `properties` matching the corresponding Zig struct fields in `src/protocol.zig` (`DreamBall`, `Relic`, `Signature`). Three Zig tests in `tools/schema-gen/spike/main.zig` (run via `zig build schemagen-spike-test`) enforce this structural correspondence. They pass on first invocation.
- **AC2 (byte-equivalence)** — green. `diff -r tools/schema-gen/spike/out/ tools/schema-gen/legacy-spike-fixture/` returns exit code 0 (empty diff). The spike consumer reproduces, byte-for-byte, the legacy generator's output for these three types.
- **AC3 (expressivity gap report)** — green. `docs/decisions/2026-04-28-codegen-spike-findings.md` records four expressivity gaps with chosen normalizations and Phase 3 follow-up notes: (1) discriminated-union over `type` literal × 7 — flat enum + per-variant `oneOf` post-pass; (2) TS template-literal — closed enum + emitter post-pass; (3) RFC3339 ⇄ tag-1 epoch-time — `x-cbor: "tag-1-epoch-time"` extension key; (4) `signed` attribute as positional 2-array — `x-cbor-attribute-encoding` extension key. Plus a "spike-scope omissions" subsection enumerating fields deliberately deferred to Story 1.2's full root schema.
- **AC4 (decision-promote paragraph)** — green. The findings doc closes with a "Spike outcome → architecture commitment" entry confirming **D-018 + D-030 are sufficient; no amendment proposed**. The chosen normalization vocabulary (`x-zig`, `x-zig-type`, `x-cbor`, `x-cbor-section`, `x-cbor-attribute-encoding`, `x-format-version`, `x-wire-type`, `x-wire-attribute`, `x-optional`, `x-ts-type`) is recorded as the canonical extension-key set for Story 1.2's full root schema.

### Problems encountered

- **Prior off-spec drift (resolved)**: per the restart instructions, the prior executor authored schemas for the wrong three types (Memory Palace types). Restart action 1 deleted `tools/schema-gen/spike/schemas/{aqueduct,mythos,trust-observation}-2.0.0.json` and authored the correct three files (`envelope-2.0.0.json`, `sealed-body-2.0.0.json`, `signature-tier-wrapper-2.0.0.json`).
- **Zig 0.16 std API churn**: initial spike consumer used `std.time.nanoTimestamp` and `std.fs.cwd().createFile` which are not present in 0.16. Migrated to the `std.Io.Threaded.global_single_threaded.io()` + `std.Io.Dir.cwd()` idiom matching `tools/schema-gen/main.zig`. The in-process timer was removed; NFR2 wall-clock is captured externally via `time zig build schemagen-spike` (0.17 s warm cache).
- **Pre-existing build failure in sibling story's workspace**: `src/wasm-host/spike/main.zig` (Story 5.1, untracked, in-progress) fails to compile because of unrelated `std.time.Timer.start()` / `std.array_list` API drift. This breaks `zig build` and `zig build smoke` transitively because both depend on the install step which links every `addInstallArtifact`. **Not in scope** for Story 1.1 per `feedback_dreamball_ac_scope_retreat` — staying out of sibling story's lane. Targeted Story-1.1 verification used `zig build schemagen-spike schemagen-spike-test test`, which succeeds (156/156 tests passed).

### Blocker Type / Detail

None for Story 1.1's own scope. Pre-existing `wasm-spike-host` build failure (Story 5.1 sibling story) noted in Problems above; that story's executor must resolve.

### Files created / modified

**Created (this restart)**:
- `tools/schema-gen/spike/schemas/envelope-2.0.0.json` — Envelope JSON Schema (DreamBall mirror).
- `tools/schema-gen/spike/schemas/sealed-body-2.0.0.json` — SealedBody JSON Schema (Relic mirror).
- `tools/schema-gen/spike/schemas/signature-tier-wrapper-2.0.0.json` — SignatureTierWrapper JSON Schema (Signature + Stage mirror).
- `tools/schema-gen/spike/main.zig` — JSON-Schema-consumer spike + AC1 round-trip tests.
- `tools/schema-gen/spike/out/types.spike.ts` — emitted (byte-equivalent to legacy fixture).
- `tools/schema-gen/spike/out/schemas.spike.ts` — emitted.
- `tools/schema-gen/spike/out/cbor.spike.ts` — emitted.
- `tools/schema-gen/legacy-spike-fixture/types.spike.ts` — extracted snapshot from `tools/schema-gen/main.zig TYPES_SRC`.
- `tools/schema-gen/legacy-spike-fixture/schemas.spike.ts` — extracted snapshot from `SCHEMAS_SRC`.
- `tools/schema-gen/legacy-spike-fixture/cbor.spike.ts` — extracted snapshot from `CBOR_SRC`.
- `docs/decisions/2026-04-28-codegen-spike-findings.md` — AC3 findings + AC4 decision-promote paragraph.

**Deleted (off-spec from prior executor)**:
- `tools/schema-gen/spike/schemas/aqueduct-2.0.0.json`
- `tools/schema-gen/spike/schemas/mythos-2.0.0.json`
- `tools/schema-gen/spike/schemas/trust-observation-2.0.0.json`

**Modified**:
- `build.zig` — added `schemagen-spike` and `schemagen-spike-test` build steps wired to `tools/schema-gen/spike/main.zig`.

### NFR2 timing

- `zig build schemagen-spike` warm cache: **0.17 s** wall-clock. Budget: ≤ 5 s. Headroom: ~30× under budget at three-schema scale. Captured via `{ time zig build schemagen-spike ; }` in the verification pass.

### Verification commands (reproducible)

```sh
# AC1 round-trip
zig build schemagen-spike-test

# AC2 byte-equivalence
zig build schemagen-spike
diff -r tools/schema-gen/spike/out/ tools/schema-gen/legacy-spike-fixture/   # exits 0

# NFR2 wall-clock
{ time zig build schemagen-spike ; }

# Story-1.1-targeted gate set (avoids the unrelated wasm-spike-host install)
zig build schemagen-spike schemagen-spike-test test    # 156/156 passed
bun run check                                           # 0 errors, 0 warnings
bun run test:unit -- --run                              # 65 files / 639 tests passed
```
