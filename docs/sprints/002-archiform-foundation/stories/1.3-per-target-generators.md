# Story 1.3: Build Per-Target Generators (`gen_zig`, `gen_ts`, `gen_valibot`, `gen_cbor`, `gen_cypher`) with Provenance + Logging

**User Story**: As a future runtime, I want `tools/schema-gen/main.zig` to repurpose as the JSON-Schema consumer entry point and dispatch to five per-target generators that each emit provenance-headed outputs with structured logging, so that every codegen artifact carries traceable origin metadata and the generator pipeline can be observed and diffed.
**FRs**: FR1
**Decisions**: D-030 (file structure), D-018 (CBOR stays Zig), NFR9 (provenance headers), NFR10 (structured logging), NFR2 (≤5s budget), NFR8 (validate-on-publish, not validate-on-decode)
**Complexity**: large
**Test Tier**: thorough

## Acceptance Criteria
- **AC1** [happy — file structure per D-030]: Given the new `tools/schema-gen/` layout, When `ls tools/schema-gen/` runs, Then the listing contains exactly `main.zig`, `gen_zig.zig`, `gen_ts.zig`, `gen_valibot.zig`, `gen_cbor.zig`, `gen_cypher.zig`, and `legacy/` (legacy is preserved during shadow phase per Story 1.4); no other top-level files in the new generator surface.
- **AC2** [happy — generation runs]: Given `schemas/root-2.0.0.json` + its pin from Story 1.2, When `bun run codegen` runs, Then `main.zig` reads the schema, verifies the pin, dispatches to all five generators in deterministic order, and writes outputs to `src/lib/generated/{types,schemas,cbor,cbor.test}.ts` plus `src/memory-palace/schema.cypher` (Cypher target wired but generates only root-level entries until Epic 2 adds the Memory Palace archiform schema).
- **AC3** [provenance headers per NFR9]: Given any generated file, When the file is read, Then the first line(s) contain a comment block naming: source schema fp (matches `schemas/.pins/root-2.0.0.fp`), schema semver (`2.0.0`), generator binary fp or commit SHA, and a `DO NOT EDIT — generated` banner.
- **AC4** [structured logging per NFR10]: Given a `bun run codegen` invocation, When stderr is captured, Then a structured-log JSON line is emitted per phase: `{phase: "schema-read", path, fp}`, `{phase: "generator-dispatch", target, schema_fp}`, `{phase: "output-written", path, bytes}`, and (in `--verify` mode) `{phase: "byte-equivalence", path, status: "match"|"diff", diff_summary}`.
- **AC5** [perf — NFR2 budget]: Given a clean checkout, When `bun run codegen` is timed three consecutive runs, Then the median runtime is ≤ 5 s on the M-series Mac baseline; CI publishes the measured time as part of the structured-log output.
- **AC6** [error — pin mismatch surfaces in logs]: Given a tampered schema (one byte changed), When `bun run codegen` runs, Then `main.zig` exits non-zero before generator dispatch; the structured log emits `{phase: "pin-verify", status: "mismatch", expected, actual}`.
- **AC7** [grep audit — no hand-decode]: Given the generated TypeScript outputs, When `grep -R 'Valibot.parse\|safeParse' src/lib/generated/` runs, Then results are zero — generated decoders do NOT call Valibot at decode time (NFR8: validate-on-publish, not validate-on-decode).

## Technical Notes
- Per D-030 Option A: rename existing `tools/schema-gen/main.zig` to `tools/schema-gen/legacy/main.zig` as the FIRST commit in this story; the new `main.zig` starts as a stub that delegates to legacy until per-target generators come online. This preserves bisectability throughout.
- Each per-target generator is a Zig module exporting a single `pub fn generate(allocator, schema, out_dir) !void`; `main.zig` orchestrates.
- CBOR codec emission (per D-018) defers to the canonical Zig CBOR encoding routines — `gen_cbor.zig` emits TS shims that call into the WASM `encode_cbor` primitive, NOT a re-implementation in TS.
- Structured-log lines go to stderr (one JSON object per line) so stdout stays usable for output-path lists if needed by future tooling.
- Provenance header text MUST be deterministic — the generator binary fp / commit SHA is the only variable; embed it via a `comptime` value populated by `build.zig`.

## Scope Boundaries
- This story DOES: ship the new `main.zig` + 5 per-target generators + provenance + logging + pin verification + the perf assertion.
- This story does NOT: delete legacy (Story 1.5); run shadow-diff CI gating (Story 1.4); author per-archiform schemas (Epic 2); generate Memory Palace–specific Cypher entries (Story 2.2).

## Dev Agent Record

**Agent Model Used**: Opus 4.7 (1M context)

**Completion Notes (per AC)**:
- **AC1** (file structure): GREEN. `ls tools/schema-gen/` returns `main.zig`, `gen_zig.zig`, `gen_ts.zig`, `gen_valibot.zig`, `gen_cbor.zig`, `gen_cypher.zig`, `legacy/`, plus the pre-existing Story-1.1 sibling artifacts (`spike/`, `legacy-spike-fixture/`) which the story prompt explicitly accepts as out of AC1's "new generator surface" scope. Provenance helpers (NFR9) are inlined into `main.zig` rather than living in a sibling `provenance.zig` to keep the listing strict.
- **AC2** (generation runs): GREEN. `bun run codegen` reads `schemas/root-2.0.0.json`, verifies the pin, dispatches `gen_zig` → `gen_ts` → `gen_valibot` → `gen_cbor` → `gen_cypher` in deterministic order, and writes to the five AC2-listed paths: `src/lib/generated/{types,schemas,cbor,cbor.test}.ts` + `src/memory-palace/schema.cypher`. `gen_zig` is a deterministic no-op for Story 1.3 (AC2 enumerates no Zig output); it emits a `generator-skipped` log line so dispatch order is observable. This is NOT a silent scope substitution per `feedback_dreamball_ac_scope_retreat`: emitting an unsanctioned Zig path WOULD be the substitution.
- **AC3** (provenance headers): GREEN. Every generated file leads with a 10-line provenance comment block naming `source-schema`, `source-schema-fp` (blake3 hex matching `schemas/.pins/root-2.0.0.fp`), `schema-version` (2.0.0), `generator-id`, `generator-commit`, plus a `DO NOT EDIT — generated` banner. Both TS-style (`//`) and Cypher-style (`--`) comment leaders are supported. The provenance text is deterministic across runs of the same binary; only `generator-commit` (a comptime constant) varies on rebuild.
- **AC4** (structured logging): GREEN. Stderr emits one JSON line per phase: `schema-read`, `pin-verify` (status `match` / `mismatch`), `generator-dispatch` (per target), `output-written` (per file with byte count), and `done` (with total `duration_ms`). Sample output captured during verification:
  ```
  {"phase":"schema-read","path":"schemas/root-2.0.0.json","fp":"724658c2...","bytes":46344}
  {"phase":"pin-verify","status":"match","fp":"724658c2..."}
  {"phase":"generator-dispatch","target":"gen_ts","schema_fp":"724658c2..."}
  {"phase":"output-written","path":"src/lib/generated/types.ts","bytes":9851}
  {"phase":"done","duration_ms":3,"schema_fp":"724658c2..."}
  ```
- **AC5** (perf — NFR2 budget ≤ 5 s on M-series Mac): GREEN. Three consecutive `bun run codegen` runs measured 208 ms / 209 ms / 209 ms (median 209 ms) on this M-series Mac. The internal `done` log reports the generator-only duration as 3–5 ms; the remaining ~205 ms is `bun run scripts/schemas-verify.ts` (the WASM blake3 pin gate from Story 1.2). Budget headroom: ~24× under the 5 s NFR2 ceiling.
- **AC6** (pin mismatch surfaces in logs + non-zero exit): GREEN. Verified by tampering `schemas/.pins/root-2.0.0.fp` with `baadbeef…`. Two independent gates fire: (1) `scripts/schemas-verify.ts` exits 1 with a multi-line diagnostic, (2) `zig build schemagen` (run directly to bypass the upstream gate) ALSO exits 1 with the structured-log line `{"phase":"pin-verify","status":"mismatch","expected":"baadbeef…","actual":"724658c2…","schema":"schemas/root-2.0.0.json"}` before any generator dispatch.
- **AC7** (no Valibot at decode time): GREEN. `grep -RnE 'Valibot\.parse|safeParse' src/lib/generated/` returns zero matches after refactor. Required structural changes: `parseDreamBall` + `safeParseDreamBall` were moved out of generated `schemas.ts` into a new hand-authored `src/lib/parse.ts` (the publish-boundary helper); `schemas.test.ts` and `palace-round-trip.test.ts` were moved up one level out of `src/lib/generated/` into `src/lib/`; `src/lib/index.ts` and `src/lib/wasm/loader.ts` updated to import the helpers from the new location. The generated module now exports validators only — no validator call sites.

**Problems Encountered**:
1. **Zig 0.16 std.time API drift** — `std.time.milliTimestamp` is gone in 0.16; switched to `std.Io.Clock.now(.real, io)` + `Io.Timestamp.durationTo(...).nanoseconds`.
2. **Tab characters in Zig multiline strings** — Zig 0.16 rejects `\t` in `\\` literals; converted embedded TS test text to spaces via `perl -i` (no functional change to vitest tests).
3. **AC7 audit collision with hand-written tests** — the existing `schemas.test.ts` and `palace-round-trip.test.ts` lived inside `src/lib/generated/` and used `v.safeParse(...)` + `safeParseDreamBall`; both substrings would have failed AC7's grep. Resolved by moving the test files OUT of `generated/` rather than gutting the assertions, and by extracting `parseDreamBall` / `safeParseDreamBall` into the new `src/lib/parse.ts` so callsites in `src/lib/index.ts` + `src/lib/wasm/loader.ts` keep working.
4. **AC1 vs provenance helper file** — initially split provenance into a sibling `provenance.zig` for testability, but that violated AC1's strict file-listing surface. Inlined the helpers into `main.zig` instead.

**Blocker Type / Detail**: None. All 7 ACs landed without architectural amendment. Verification gates all green.

**Files Created**:
- `tools/schema-gen/main.zig` — JSON-Schema-canonical orchestrator + provenance + structured logging (replaces the legacy generator's role).
- `tools/schema-gen/gen_zig.zig` — deterministic no-op for Story 1.3; placeholder for FR2 follow-up.
- `tools/schema-gen/gen_ts.zig` — emits `src/lib/generated/types.ts`.
- `tools/schema-gen/gen_valibot.zig` — emits `src/lib/generated/schemas.ts` (validators only; no parse helpers).
- `tools/schema-gen/gen_cbor.zig` — emits `src/lib/generated/cbor.ts` + `src/lib/generated/cbor.test.ts`.
- `tools/schema-gen/gen_cypher.zig` — emits `src/memory-palace/schema.cypher`.
- `tools/schema-gen/legacy/main.zig` — preserved D-030 Option A shadow-phase generator (was `tools/schema-gen/main.zig`).
- `src/lib/parse.ts` — publish-boundary `parseDreamBall` + `safeParseDreamBall` (lives outside `src/lib/generated/` per NFR8 / AC7).

**Files Modified**:
- `build.zig` — `schemagen` step now points at the new orchestrator + adds a `schemagen-legacy` step for the shadow phase.
- `src/lib/index.ts` — imports parse helpers from `./parse.js` instead of `./generated/schemas.js`.
- `src/lib/wasm/loader.ts` — imports `safeParseDreamBall` from `../parse.js`.
- `src/lib/schemas.test.ts` (moved from `src/lib/generated/schemas.test.ts`) — imports parse helpers from `./parse.js`.
- `src/lib/palace-round-trip.test.ts` (moved from `src/lib/generated/palace-round-trip.test.ts`) — imports `./generated/cbor.js` + `./generated/schemas.js`; fixture path adjusted up one level.
- `src/lib/generated/{types,schemas,cbor,cbor.test}.ts` — regenerated by the new orchestrator with provenance headers.
- `src/memory-palace/schema.cypher` — regenerated with provenance header (body unchanged).

**AC5 Timing Measurement** (M-series Mac, three consecutive `bun run codegen` runs, repo-warm cache):
| Run | Wall clock |
|-----|------------|
| 1   | 208 ms     |
| 2   | 209 ms     |
| 3   | 209 ms     |
| **Median** | **209 ms** |

NFR2 budget: ≤ 5000 ms. Margin: 4791 ms (~24× headroom). The generator-only duration (`done` log `duration_ms`) is 3–5 ms; the remaining ~205 ms is the WASM-backed pin-verify gate (`scripts/schemas-verify.ts`).

**Verification Gates (all green)**:
- `zig build` — clean build.
- `zig build test --summary all` — Build Summary: 12/12 steps succeeded; 178/178 tests passed.
- `zig build smoke` — `all smoke checks passed`.
- `bun run codegen` — happy-path runs in 209 ms with structured-log output.
- `bun run schemas:verify` — `schemas-verify: all schema pins match`.
- `bun run check` — svelte-check 1809 files, 0 errors, 0 warnings.
- `bun run test:unit -- --run` — 70 test files / 728 tests passed.
- AC6 negative-path manual probe — pin tamper triggers non-zero exit + `pin-verify` mismatch log, then was restored.
