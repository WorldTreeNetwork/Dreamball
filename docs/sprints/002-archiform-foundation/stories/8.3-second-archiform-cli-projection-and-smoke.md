# Story 8.3 [STRETCH]: Project Guestbook Action to CLI Verb + Smoke Verification (Federation Proof Closed)

**User Story**: As an archiform author, I want the guestbook `sign` action mechanically projected to a `jelly guestbook sign ...` CLI verb (using the same projection generator from Stories 3.2–3.4 with NO generator changes), with a smoke test exercising the entire pipeline end-to-end, so that the federation hypothesis is proven closed: schema → pin → codegen → wasm → host → CLI projection → signed envelope, all reusing existing infrastructure.
**FRs**: (no FRs — integration test of the entire sprint manifold)
**Decisions**: D-019, D-022 (bridge pattern), D-035, IC2
**Complexity**: small
**Test Tier**: thorough

## Acceptance Criteria
- **AC1** [happy — CLI verb projects with NO projection-generator changes]: Given Stories 3.2/3.5's projection generator, When `bun run codegen` runs, Then `src/cli/generated/guestbook_sign.zig` is emitted with NFR9 provenance header naming the guestbook schema pin; the Dev Agent Record explicitly confirms NO changes to the projection generator were required.
- **AC2** [happy — `jelly guestbook sign` runs end-to-end]: Given the generated verb + Story 8.2's `sign.wasm`, When `jelly guestbook sign --author "alice" --message "hello"` runs, Then the CLI dispatcher routes through the projection layer to the wasm host, the host verifies + instantiates `sign.wasm`, the action calls `dreamball.emit_action_envelope`, the host signs via Epic 6's `signActionEnvelope`, the bridge pattern promotes the envelope to live CAS, and the CLI returns a typed JSON output; structured invocation event emitted per NFR11.
- **AC3** [happy — smoke gate]: Given the verb + a guestbook smoke fixture (added to `scripts/cli-smoke.sh` under a new `guestbook` section), When `scripts/cli-smoke.sh` runs, Then both Memory Palace and guestbook smoke sections pass; no regression in either archiform.
- **AC4** [happy — `--help` derived from manifest]: Given `jelly guestbook sign --help`, When inspected, Then output reflects the guestbook manifest's `summary` + `inputs` flags.
- **AC5** [happy — bridge pattern composed]: Given a `jelly guestbook sign` invocation with `JELLY_BRIDGE_DEBUG=1`, When traced, Then the staging → bridge → promote sequence per D-022 is observed.
- **AC6** [happy — federation proof closure]: Given Stories 8.1 + 8.2 + 8.3 land green, When the closure note is added to `docs/sprints/002-archiform-foundation/retrospective.md` (or a sprint-end-summary doc), Then it states: "the federation hypothesis is exercised end-to-end; a second archiform was authored with NO changes to the wasm host, NO changes to the projection generator, and NO new `dreamball.*` imports."
- **AC7** [happy — full gate set green]: Given the closure, When the full gate set runs, Then all 7 gates pass — sprint-002 is provably done with the federation proof.

## Technical Notes
- "NO changes to the projection generator" (AC1) and "NO host changes" (Story 8.2 AC2) and "NO new imports" (Story 8.2 AC3) are the THREE federation-proof criteria. Any deviation from these is itself a finding (the foundation has Memory-Palace-shaped leaks).
- `jelly guestbook sign` is the first time the CLI dispatcher routes a non-Memory-Palace verb. The flat-table dispatch (D-013) needs to accept a new top-level archiform key (`guestbook`) — verify this works without dispatch-table changes (or, if it requires a one-line registration, document that as a known-good extensibility seam, NOT a generator change).
- Per IC2: guestbook envelopes serialize to the existing signed-action envelope shape — no new envelope fields.
- Per Cluster I activation (per `epics.md` §Activation Criteria): if Epics 1–7 do not complete cleanly with margin, this story (and 8.1, 8.2) DROP. Phase 4 dispatch should sequence Epic 8 last and only commit to it after Epic 7 lands.

## Scope Boundaries
- This story DOES: project + smoke + close the federation proof per AC6.
- This story does NOT: ship MCP/programmatic projections for guestbook (sprint-003 if needed); add additional guestbook actions (one is enough for the proof); ship aspects.sh-side publishing (out of scope per TC6).
