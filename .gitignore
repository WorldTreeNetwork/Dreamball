node_modules
package-lock.json

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# Zig
.zig-cache/
zig-out/
zig-pkg/

# OMC
.omc/

# Claude Code local harness state
.claude/

# jelly-server runtime data (generated; contains secret keys — never commit)
jelly-server/data/
jelly-server/node_modules/

# kuzu-wasm worker — emitted by bun run bootstrap (scripts/bootstrap-kuzu-wasm.ts)
static/kuzu_wasm_worker.js

# TypeScript declarations leaked into src/ by publint/tsc during `bun run build`
# (real .d.ts output goes to /dist/; these are strays)
src/memory-palace/*.d.ts
src/lib/bridge/*.d.ts

# Debug / scratch artefacts (Playwright snapshots, ad-hoc screenshots, REPL dumps)
tmp/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

*storybook.log
storybook-static
.playwright-mcp
.firecrawl
.omc/
